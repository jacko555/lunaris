{
  "name": "@lunaris/sim-core",
  "version": "0.0.1",
  "private": true,
  "description": "Deterministic ECS simulation core for LUNARIS (pure ESM, no DOM, no engine deps)",
  "type": "module",
  "license": "MIT",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
