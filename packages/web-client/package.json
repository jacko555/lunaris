{
  "name": "@lunaris/web-client",
  "version": "0.0.1",
  "private": true,
  "description": "LUNARIS web client (Vite; PixiJS renderer arrives in Milestone 2)",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@lunaris/sim-core": "workspace:*",
    "pixi.js": "^8.6.2"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
