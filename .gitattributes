# Normalize to LF in the repo and on checkout — keeps hashes and diffs
# identical across Windows/macOS/Linux dev machines and the CI matrix.
* text=auto eol=lf
*.png binary
*.jpg binary
*.webp binary
*.ogg binary
*.mp3 binary
*.woff2 binary
